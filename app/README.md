# App directory — run instructions

From this folder:

```bash
pip install -r requirements.txt
copy .env.example ..\.env   # or copy to .env here
# Set GROQ_API_KEY in .env
streamlit run main.py
```

See the root [README.md](../README.md) for full documentation, Render deployment, and sample file guidance.
